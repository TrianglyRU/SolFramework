// Inherit the parent event
event_inherited();

depth = RENDERER_DEPTH_HIGHEST;
image_index = vd_countdown_frame;
offset_x = array_create(CAMERA_COUNT, undefined);
offset_y = array_create(CAMERA_COUNT, undefined);
display_timer = 0;